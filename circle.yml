machine:
  node:
    version: 5.5.0

dependencies:
  post:
    - npm install eslint@2.2.0 #actual garbage

test:
  override:
    - npm run lint
    - npm run build

deployment:
  master:
    branch: master
    owner: rit-sse
    commands:
      - ssh deploy@$SSE_HOST 'cd /docker && docker-compose restart scoreboard nginx'
